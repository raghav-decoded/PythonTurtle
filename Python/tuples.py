# Tuples
# Faster and its values cannot be changed. 
tup = ( "Lightspd.","299792458 m/s")
print(tup[1])
print(tup[0:2]) # accessing chunks of tuples .
lst = [1,2,3,4]
tuple(lst) # Converting a list to tuple .
list(tup) # Converting a tuple to a list .

      